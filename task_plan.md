# 校园成长平台上线计划

日期：2026-04-24

## 目标

备案通过后，尽快把当前 H5 主线推到可访问、可审核、可运营的线上版本，并以最短路径提交微信小程序审核。

## 当前判断

当前仓库主线是 React + Vite H5 前端和 Express API，不是完整原生微信小程序工程。最快上线方式是：

1. 先把 H5 切到正式域名、HTTPS、真实数据库、真实 OSS、真实微信登录。
2. 新建一个极薄微信小程序壳，用 `web-view` 承载正式 H5。
3. 通过小程序后台配置业务域名、request 合法域名、隐私协议和审核材料后提交审核。

## 阶段 0：上线前安全止血

状态：pending

- 轮换已暴露的 OSS AccessKey、微信 AppSecret、管理员默认密码。
- 清理 `.env.example` 中真实凭证，只保留占位值。
- 清理源码中的中文乱码和默认用户资料乱码。
- 确认支付继续暂缓，用户端不出现购买、支付、退款入口。

## 阶段 1：正式域名与 HTTPS

状态：pending

- 将备案通过的域名解析到 `121.43.58.9`。
- 配置 Nginx HTTPS，签发证书。
- 将 `API_PUBLIC_ORIGIN` 切到正式域名。
- 验证 `/`、`/api/health`、静态资源、上传资源、下载授权链路。

## 阶段 2：微信登录与小程序壳

状态：pending

- 将前端 `demo-code` 快速登录替换成真实微信登录桥接。
- 新增小程序 shell 工程，入口页使用 `web-view` 打开正式 H5。
- 小程序壳调用 `wx.login` 获取 code，将 code 传给 H5 登录页或后端。
- 后端继续使用已有 `/api/auth/wechat/login` 和 `jscode2session`。
- 微信后台配置 request 合法域名和 web-view 业务域名。

## 阶段 3：审核合规与产品收口

状态：pending

- 补用户协议、隐私政策、个人信息收集说明。
- 小程序后台填写用户隐私保护指引。
- 确保用户生成内容有审核、举报、下架、管理员处理闭环。
- 清理演示数据、开发态文案、乱码、支付相关文案。
- 做审核用首页运营内容和首批公开内容。

## 阶段 4：生产运维

状态：pending

- PostgreSQL 每日备份和恢复演练。
- OSS 桶权限复核，资源走后端授权下载。
- Nginx/API 日志轮转。
- API 限流、上传大小和文件类型复核。
- 准备回滚方案和发布检查清单。

## 阶段 5：提交审核与灰度上线

状态：pending

- 微信开发者工具真机预览。
- 管理员、游客、首次登录、资料未完善、普通用户全链路冒烟。
- 提交体验版和审核版。
- 审核通过后先小范围灰度，再正式推广。

