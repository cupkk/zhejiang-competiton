# 校园成长平台商业化计划 2026-05-03

## 1. 商业化定位

第一阶段定位为“免费引流 + 校园合作转化”。平台先提供竞赛信息、免费资源、组队协作、经验社区和后台审核能力，不开放在线支付、会员订阅、退款和发票流程。

核心目标不是马上卖课或卖资料，而是先拿到真实校园用户、真实内容供给和校园组织合作线索。第一阶段商业化验证只看三件事：

- 是否有学生持续浏览、收藏、领取资源和发起组队。
- 是否有老师、社团、学院或竞赛团队愿意提供内容和推广入口。
- 是否能通过校园合作包、运营服务或定制资源库形成可签约收入。

## 2. 第一阶段变现路径

### 免费引流

- 首页、竞赛、资源、组队、社区保持免费可浏览。
- 资源大厅默认只展示免费且审核通过的资源。
- 免费资源支持领取和下载授权，用来沉淀用户兴趣、专业方向和竞赛意向。
- 付费资源在第一阶段不公开展示，直链访问返回“该资源暂未公开”。

### 校园合作转化

- 面向学院、社团、创新创业中心、竞赛工作室提供“竞赛运营包”。
- 合作内容包括竞赛专题页、资源整理、报名引导、组队收集、答疑帖运营、数据复盘。
- 早期可采用项目制收费：按单场竞赛、单学期或单学院合作报价。
- 保留后台审核、首页运营位和投稿管理，支撑人工运营交付。

### 后续付费能力

- 在线支付、会员订阅、自动退款、发票流程不进入第一阶段。
- 支付代码保留但默认关闭，只有在资质、售后、税务和微信支付真机联调完成后再启用。

## 3. 当前线上状态

### 已完成

- 已发布新 release：`/opt/campus-growth/releases/20260503133215`。
- `http://campusgrow.top/` 已 301 跳转到 `https://campusgrow.top/`。
- `https://campusgrow.top/` 已返回 200，并带有 HSTS、X-Content-Type-Options、X-Frame-Options、Referrer-Policy。
- `https://www.campusgrow.top/` 已返回 200。
- Let’s Encrypt 证书已覆盖 `campusgrow.top` 和 `www.campusgrow.top`，有效期至 2026-08-01。
- `certbot-renew.timer` 已启用，`certbot renew --dry-run` 通过。
- 线上 `/api/health` 已返回 `databaseProvider=postgres`、`storageProvider=s3`、`paymentsEnabled=false`。
- 线上 `API_PUBLIC_ORIGIN=https://campusgrow.top`，`PAYMENTS_ENABLED=false`。
- 线上 `WECHAT_LOGIN_MODE=real` 已启用，微信 AppID/AppSecret 已只写入服务器环境变量，不进入仓库文档。
- 线上 `/api/health` 已返回 `wechatLoginMode=real`。

### 仍未完成

- 微信后台 request 合法域名和 web-view 业务域名必须配置为 `https://campusgrow.top`。
- 如果微信后台要求业务域名校验文件，需要上传到站点根目录并复核 HTTPS 可访问。
- 小程序壳仍需真机联调 `wx.login`、web-view 跳转、登录态保持和业务冒烟。

## 4. 当前不足清单

### 安全与运维不足

- 服务器环境变量、微信 AppSecret、管理员默认密码必须轮换或确认未泄漏。
- `.env.example` 已改为占位值，但生产 env 仍需逐项复核。
- PostgreSQL 备份策略、恢复演练、日志轮转和磁盘告警未完成验收。
- S3 桶需要确认为私有桶，下载必须走后端授权。
- Nginx 需要配置安全头、上传大小限制、静态资源缓存和 HTTP 到 HTTPS 跳转。

### 产品体验不足

- AI/规划能力仍是内测功能，主入口默认隐藏。
- 历史订单页保留为历史记录路径，不进入用户主路径。
- 浏览历史、客服反馈仍偏轻，需要在灰度期接入真实运营流程。
- 首页运营内容需要使用真实校园图片和真实竞赛/资源，而不是占位素材。

## 5. 已完成的商业化改造

- 新增 `PAYMENTS_ENABLED=false` 服务端配置。
- 新增 `VITE_PAYMENTS_ENABLED=false` 前端配置。
- `/api/health` 增加 `paymentsEnabled`，便于上线验收。
- 支付关闭时，付费资源获取、订单支付、订单退款返回 `payments_disabled`。
- 用户端资源列表默认展示免费资源。
- 后端在支付关闭时过滤公开付费资源，搜索和首页推荐不再暴露付费资源。
- 用户端移除 demo-code 快速登录入口，登录改为微信小程序 `wx.login` code。
- 用户端隐藏浮动 AI 快捷入口，规划页保留为内测路由。
- 首页、竞赛、资源、社区、资源详情、账号设置等页面已按 iOS 风格做轻量化。
- 清理用户端“演示链路”“备案通过后”“后续会”“暂未开放购买”等开发态文案。
- `.env.example` 和 `frontend/.env.example` 已加入商业化默认配置和占位密钥。

## 6. 上线硬门槛

### 服务器连接

后续服务器执行使用：

```powershell
ssh -i D:/github/zhejiang-competiton/.deploy/school_ssh.pem ecs-user@121.43.58.9
```

### 域名与 HTTPS

- `campusgrow.top` 和 `www.campusgrow.top` 均配置 HTTPS。
- HTTP 全量 301 跳转 HTTPS。
- 443 端口对公网开放。
- 证书使用 Let’s Encrypt 或云厂商证书，并配置自动续期。

### 生产环境变量

已完成或待补的生产环境变量：

```env
API_PUBLIC_ORIGIN=https://campusgrow.top  # 已完成
PAYMENTS_ENABLED=false                    # 已完成
DB_PROVIDER=postgres                      # 已完成
STORAGE_PROVIDER=s3                       # 已完成
WECHAT_LOGIN_MODE=real                    # 已完成
```

微信 AppID/AppSecret 只保存在服务器 `/etc/campus-growth/api.env`，不写入仓库、计划文档或示例配置。

### 数据与文件

- PostgreSQL 至少每日备份，保留 7 天以上。
- S3 桶不可公网列目录，不允许直连下载私有资源。
- 上传文件限制类型和大小，异常上传可追踪。
- 资源下载必须通过后端授权链接。

### 微信审核

- request 合法域名：`https://campusgrow.top`。
- web-view 业务域名：`https://campusgrow.top`。
- 小程序壳调用 `wx.login` 后把 code 传给 `/api/auth/wechat/login`。
- 隐私政策覆盖登录、资料、收藏、组队、投稿、消息和文件上传。

## 7. 运营指标

### 北极星指标

每周有效行动用户数：一周内完成任意两项动作的用户数。动作包括收藏、免费资源领取、下载、组队申请、发帖、评论、投稿。

### 核心漏斗

- 访问首页人数。
- 搜索或点击竞赛/资源人数。
- 收藏竞赛或资源人数。
- 免费资源领取人数。
- 资源下载人数。
- 发起或申请组队人数。
- 发帖、评论或举报人数。
- 后台审核处理量。

### 合作转化指标

- 每周新增校园合作线索数。
- 合作方提供内容数。
- 合作专题访问人数。
- 合作专题报名或组队转化数。
- 单场竞赛运营交付复盘完成率。

## 8. 30/60/90 天路线图

### 30 天：可灰度推广

- 完成 HTTPS、真实微信登录、小程序壳和生产环境变量。
- 完成管理员密码、微信密钥、S3 凭据轮换。
- 完成 20 个高质量竞赛条目和 30 份免费资源。
- 完成首页运营位、资源审核和内容发布 SOP。
- 小范围邀请 1-2 个校园社群试用。

### 60 天：验证校园合作

- 与 2-3 个社团、学院项目组或竞赛组织建立合作。
- 为重点竞赛制作专题页、资料包和组队入口。
- 建立每周数据复盘：访问、领取、组队、发帖、审核。
- 完成反馈入口和运营处理台账。
- 梳理第一版合作报价和交付清单。

### 90 天：形成可复制交付

- 沉淀 3 个以上合作案例。
- 完成校园合作包标准报价、合同条款和交付验收模板。
- 优化推荐、搜索和资源分发，让用户能更快找到相关竞赛。
- 根据真实需求决定是否启动支付二期。
- 建立生产监控、备份恢复演练和安全巡检节奏。

## 9. 风险清单

- HTTPS 未完成会直接阻塞小程序审核。
- 微信登录未切 real 会导致正式用户体系不可用。
- 支付入口误暴露会带来售后、退款和合规风险。
- 密钥未轮换会带来账户、存储和用户数据风险。
- 资源版权不清会影响校园合作和平台信誉。
- AI 功能过早开放会带来生成内容合规和质量风险。
- 内容审核不足会导致帖子、资源和组队信息质量不可控。

## 10. 验收标准

### 本地验收

- `npm run lint` 通过。
- `npm run typecheck:frontend` 通过。
- `npm run build:frontend` 通过。
- `/api/health` 返回 `paymentsEnabled=false`。
- 用户端首页、竞赛、资源、资源详情、登录、我的、社区、规划页无开发态文案。
- 免费资源可领取和下载授权。
- 付费资源不会创建待支付订单。

### 线上验收

- `curl -I http://campusgrow.top/` 返回 301 到 HTTPS。
- `curl -I https://campusgrow.top/` 返回 200。
- `curl https://campusgrow.top/api/health` 已返回 `postgres`、`s3`、`real`、`paymentsEnabled=false`。
- 小程序真机完成登录、浏览、收藏、免费资源获取、组队申请、发帖、消息和后台审核冒烟。

## 11. 商用前保留事项

- 支付二期不得在无合同、退款、发票和客服 SOP 前开启。
- 规划页正式开放前必须接入真实模型、内容边界和审核策略。
- 校园合作前必须准备隐私政策、用户协议、内容版权确认和合作报价单。
- 每次上线前必须复核 `.env.example`、生产 env、Nginx、systemd、数据库备份和 S3 权限。
