# 阿里云邮件短信接入指南 20260708

## 目标

当前学校认证页已经有教育邮箱验证码和手机号验证码的业务流程，但还没有接入真实发送服务。内测前建议先保留调试模式；正式灰度时切到阿里云短信服务和邮件推送，所有验证码由后端统一发送、限流和审计。

## 选型

- 短信：阿里云短信服务，调用 `Dysmsapi 2017-05-25 SendSms`。
- 邮件：阿里云邮件推送 Direct Mail。优先用 OpenAPI/SDK；如果接口文档路径变化，可退回 SMTP Node.js 接入，后端封装保持不变。
- 密钥：只用 RAM 用户 AccessKey，不使用主账号 AccessKey。
- 场景：只发送学校认证验证码，不做营销、订阅或批量通知。

官方参考：

- [SendSms - 发送短信](https://help.aliyun.com/zh/sms/developer-reference/api-dysmsapi-2017-05-25-sendsms)
- [SendSms OpenAPI 调试页](https://api.aliyun.com/api/Dysmsapi/2017-05-25/SendSms)
- [SingleSendMail OpenAPI 调试页](https://api.aliyun.com/api/Dm/2015-11-23/SingleSendMail)
- [申请短信签名](https://help.aliyun.com/zh/sms/user-guide/create-signatures)
- [邮件推送产品文档](https://help.aliyun.com/zh/direct-mail/)
- [设置发信域名](https://help.aliyun.com/zh/direct-mail/user-guide/how-to-configure-sending-domain-names)
- [RAM 用户与 AccessKey](https://help.aliyun.com/zh/ram/)

## 控制台准备

1. 完成阿里云账号实名认证，建议企业主体认证。
2. 创建 RAM 用户，例如 `campus-growth-verification-sender`。
3. 给 RAM 用户最小权限：
   - 短信只需要 `dysms:SendSms`。
   - 邮件只给邮件推送发送相关权限。
   - 不授予 ECS、数据库、OSS、账户管理权限。
4. 创建 AccessKey 后只写入服务器环境变量，不提交到 Git。
5. 打开短信服务，申请资质、短信签名和验证码模板。
6. 打开邮件推送，配置发信域名并完成 DNS 验证。

## 短信服务配置

短信模板建议内容：

```text
验证码：${code}。用于校园成长平台学校认证，10分钟内有效，请勿泄露。
```

控制台要拿到：

- `SignName`：审核通过的短信签名。
- `TemplateCode`：审核通过的验证码模板编码。
- `Endpoint`：国内短信默认 `dysmsapi.aliyuncs.com`。

后端调用 `SendSms` 时只传单个手机号。官方文档说明 `SendSms` 支持多号码，但验证码建议单条发送，平台也更容易做限流和审计。

关键请求参数：

- `PhoneNumbers`：手机号。
- `SignName`：短信签名。
- `TemplateCode`：模板编码。
- `TemplateParam`：JSON 字符串，例如 `{"code":"123456"}`。

## 邮件推送配置

建议使用二级域名作为发信域名，例如：

```text
mail.campusgrow.top
```

控制台要完成：

- 添加发信域名。
- 按阿里云页面提示配置 DNS 记录，通常包含 SPF、DKIM、MX 或 CNAME 验证项。
- 创建发信地址，例如 `no-reply@mail.campusgrow.top`。
- 等待域名状态通过。

OpenAPI 发送时重点关注：

- `AccountName`：已验证的发信地址。
- `FromAlias`：发信人显示名，例如“校园成长”。
- `ToAddress`：目标教育邮箱。
- `Subject`：邮件标题。
- `HtmlBody` 或 `TextBody`：正文。
- `ReplyToAddress`：验证码邮件建议关闭或固定为运营邮箱。

邮件内容建议保持短：

```text
标题：校园成长学校认证验证码

你的验证码是：123456

该验证码用于校园成长平台学校认证，10分钟内有效。若非本人操作，请忽略。
```

## 环境变量

`.env.example` 已新增占位符：

```env
VERIFICATION_PROVIDER=aliyun
VERIFICATION_DEBUG_CODE_VISIBLE=false
ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=
ALIYUN_SMS_ENDPOINT=dysmsapi.aliyuncs.com
ALIYUN_SMS_SIGN_NAME=
ALIYUN_SMS_TEMPLATE_CODE=
ALIYUN_DIRECTMAIL_ENDPOINT=dm.aliyuncs.com
ALIYUN_DIRECTMAIL_ACCOUNT_NAME=
ALIYUN_DIRECTMAIL_FROM_ALIAS=校园成长
```

线上服务器写入：

```bash
sudoedit /etc/campus-growth/api.env
sudo systemctl restart campus-growth-api
```

不要把 AccessKey 写入 `.env.example`、日志、文档或前端环境变量。

## 后端实现建议

新增一个发送器接口，学校认证服务只依赖接口，不直接依赖阿里云 SDK：

```ts
export interface VerificationSender {
  sendEmailCode(target: string, code: string, context: { schoolName: string }): Promise<void>;
  sendSmsCode(target: string, code: string, context: { schoolName: string }): Promise<void>;
}
```

建议文件：

- `server/verification-sender.ts`
- `server/aliyun-verification-sender.ts`
- `server/debug-verification-sender.ts`

`server/school-service.ts` 当前负责生成验证码和写入 `school_verification_codes`，后续只需要在写入成功后调用发送器。调试环境可继续返回 `debugCode`；`WECHAT_LOGIN_MODE=real` 和 `VERIFICATION_PROVIDER=aliyun` 时不返回明文验证码。

## 限流与安全

上线前必须补齐：

- 同一手机号或邮箱：60 秒内最多 1 次，24 小时最多 10 次。
- 同一用户：10 分钟最多 3 次，24 小时最多 20 次。
- 同一 IP：10 分钟最多 20 次。
- 验证码有效期 10 分钟。
- 验证失败最多 5 次，超过后重新发送。
- 数据库只存验证码 hash，不存明文。
- 日志记录 provider、channel、target hash、结果码，不记录验证码明文。
- 阿里云失败要返回通用错误文案，内部保留 requestId 便于排查。

## 推荐实施顺序

1. 在阿里云完成短信签名和模板审核。
2. 配置邮件推送发信域名，等 DNS 验证通过。
3. 创建 RAM 用户和 AccessKey，写入服务器 `/etc/campus-growth/api.env`。
4. 后端增加 `VerificationSender` 抽象和阿里云实现。
5. 本地用测试手机号和邮箱跑冒烟。
6. 线上只给管理员账号白名单试发。
7. 确认日志、限流、失败重试和监控后，再开放给内测用户。

## 验收

- 教育邮箱点击发送后，收件箱能收到验证码，页面不显示 debug code。
- 手机号点击发送后，手机能收到短信，模板内容无营销文案。
- 错误手机号、错误邮箱、超频发送会被拦截。
- 阿里云控制台能查到发送记录。
- API 日志不出现验证码明文和 AccessKey。
- `school_verification_codes` 中 `code_hash` 有值，`consumed_at` 在验证成功后写入。

## 当前项目状态

本次已完成配置和文档准备，真实发送实现还未接入。下一步可按本文新增 `VerificationSender`，然后把 `server/school-service.ts` 中的验证码生成流程接到阿里云发送器。
