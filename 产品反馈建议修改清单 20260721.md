# 产品反馈建议修改清单

## 结论

本次审阅范围包括 `参考/产品手册1.docx` 及 `参考` 目录中的首页、岗位页、攻略区、竞赛页 HTML。登录页已完成，不再重复评估。

建议保留现有产品的简洁视觉、公开联系方式组队方式和免费内容策略，重点吸收原型中“组队优先、信息结构化、校内内容隔离、竞赛详情闭环”的设计。以下仅列建议实施的修改。

## 实施状态（2026-07-22）

- 第 1-4 项已完成：内容范围显式化、双认证校内访问门槛、平台/学校后台分级、双学校隔离矩阵和本校首页内容均已形成后端闭环。
- 第 5-7 项已完成：组队大厅支持“找队伍 / 求加入”，结构化字段、完整招募正文、公开联系方式、到期下架、详情分区和安全提示均已接入真实数据。
- 第 8-9 项已完成：竞赛分类/级别独立筛选，浏览与收藏真实排序，报名和比赛时间、赛制阶段、官方通知附件、来源和核验时间均为结构化字段。
- 第 10-11 项已完成：社区问答支持待回答、已解决和采纳回答；本地一键启动、浙江大学/复旦大学测试数据和固定发布前检查均已落地。
- 最终验证：`npm run verify:local-release` 通过；390x844、414x896 用户端和 1440x900 管理端无横向溢出，浏览器控制台无错误或警告。

## P0：上线前先完成

### 1. 收紧学校内容访问规则

当前状态：

- 用户选择学校后会创建 `user_school_memberships` 记录，并立即成为当前激活学校。
- 帖子、组队和用户投稿资源发布时会写入当前 `school_id`。
- 列表、详情、搜索和收藏大多通过当前激活学校过滤。
- 但未认证学校也能成为激活学校并读取校内内容；`school_id` 为空的内容默认对所有学校公开。

建议修改：

- 明确两种内容范围：`platform` 和 `school`，不要继续只用 `school_id IS NULL` 隐式判断公共内容。
- 全国官方竞赛、官方公告和平台整理的公共资料使用 `platform` 范围，所有学校可见。
- 学生帖子、组队招募、学生投稿资源和学校公告使用 `school` 范围，只允许当前学校成员查看。
- 未完成学校认证的用户只能查看平台公共内容；教育邮箱和手机号认证完成后才能查看校内内容。
- 统一列表、详情、搜索、收藏、首页推荐、关联内容和消息跳转的范围判断，避免某个入口绕过学校隔离。
- 对历史 `school_id` 为空的数据做一次迁移：官方内容标记为 `platform`，无法确认来源的用户内容进入后台待确认队列。

验收标准：

- 浙江大学认证用户无法通过列表、搜索、收藏或详情直链读取其他学校的帖子、资源和组队。
- 未认证用户无法读取任何学校私有内容。
- 全国官方竞赛仍可被所有学校正常查看。

### 2. 完善平台后台与学校后台分级

当前状态：

- `school_admin` 已绑定学校，后端审核和举报接口会强制使用管理员学校范围。
- 平台管理员可以按学校筛选；学校管理员无法越权审核其他学校内容。
- 但学校管理员仍能看到平台首页配置入口和平台级首页统计，容易产生权限认知混乱。
- 学校管理员账号目前主要通过脚本创建，审计日志只有数据表和读取函数，没有管理界面。

建议修改：

- 学校管理员登录后固定展示学校名称，首页改为本校运营总览：待审核帖子、待审核组队、资源投稿、举报和本校内容数据。
- 学校管理员隐藏平台首页配置入口，不展示平台公共资源位和全局轮播状态。
- 为学校管理员增加本校公告和本校推荐内容配置，仅能选择本校已审核内容。
- 平台后台增加“学校管理”页面：查看学校状态、创建/停用学校管理员、重置管理员密码、查看最近登录和权限。
- 平台后台增加审计日志页面，支持按学校、管理员、操作类型和时间筛选。
- 所有管理员页面继续以后端权限判断为准，前端隐藏按钮只作为体验优化。

验收标准：

- 学校管理员只能管理本校内容和本校运营位。
- 平台管理员可跨校查看和处理，并能追溯每次审核操作。

### 3. 增加学校隔离自动化测试矩阵

建议修改：

- 本地准备浙江大学、复旦大学两个测试学校，各建立普通用户、已认证用户和学校管理员。
- 覆盖竞赛、资源、帖子、组队、搜索、收藏、详情直链、举报、审核和首页推荐。
- 每个接口同时验证“同校可见、异校不可见、平台公共可见、作者可查看自己的待审内容”。
- 将测试加入上线前固定检查，避免后续新增页面时漏加学校过滤。

## P1：优先改善核心体验

### 4. 首页补齐本校组队和本校经验内容

当前状态：

- 后端首页已经返回 `latestTeams` 和 `featuredPosts`，前端首页目前只展示推荐竞赛和热门资源。

建议修改：

- 首页顺序调整为：搜索、快捷入口、单行公告、本校正在组队、推荐竞赛、本校经验、热门资源。
- “本校正在组队”只展示 2 条紧凑卡片，突出竞赛、缺口角色、当前人数和截止时间。
- “本校经验”只展示 2 条已审核帖子，突出标题、分类、作者和互动量。
- 公告使用单行滚动或切换条，点击进入公告详情；保留一张真实校园图，但不再让公告占用大面积卡片。
- 首页不堆完整组队正文，详细信息进入组队详情查看。

### 5. 组队大厅增加“找队伍 / 求加入”双模式

建议修改：

- 保留当前队长发布招募的“找队伍”。
- 增加用户主动发布的“求加入”，由用户自愿填写擅长方向、目标竞赛、可投入时间、个人简介和公开联系方式。
- 两类内容都按当前学校隔离、进入审核队列并设置有效期，到期自动下架。
- 不开放站内私聊；联系仍通过发布者自愿公开的微信、邮箱等方式完成。
- “求加入”展示的是用户主动发布的信息，不直接公开所有用户档案。

### 6. 将组队发布页改为结构化表单

当前状态：

- 当前表单主要依赖标题、角色、长正文和联系方式，展示端难以稳定提取重点。

建议修改：

- 结构化采集：关联竞赛、项目简介、参赛目标、当前人数/计划人数、缺口角色、已有能力、合作方式、每周投入、招募要求、截止时间和联系方式。
- 缺口角色、已有能力和参赛目标使用标签选择，并允许补充自定义项。
- 保留“完整招募正文”用于自由描述，但不再承担所有字段。
- 发布前提供与真实详情页一致的预览，提交后进入本校审核队列。
- 关联竞赛从真实竞赛库选择，不再仅输入自由文本。

### 7. 提升组队卡片和详情的信息密度

建议修改：

- 列表卡片显示：竞赛、参赛目标、缺口角色、人数进度、截止时间和发起人基本信息。
- 详情页按“项目简介、招募岗位、已有能力、合作要求、发起人、联系方式”分区。
- 联系方式默认折叠，用户主动点击后显示并可复制，同时记录展示行为用于风控。
- 详情页底部固定“复制联系方式”，不新增私聊按钮。
- 增加统一安全提示：不转账、不缴押金、不提交身份证/银行卡等敏感信息，竞赛资格以官网为准。

### 8. 重整竞赛筛选维度

当前状态：

- 当前筛选把国家级、省级、校级和创新创业放在同一组选项中，层级和类别混在一起。
- 最热排序已有浏览量字段，但收藏量尚未作为真实排序指标。

建议修改：

- 分类独立为：创新创业、数学建模、编程算法、电子硬件、设计艺术、学术科研等，并与登录引导的关注方向保持同一标签池。
- 级别独立为：国家级、省级、校级。
- 排序保留：推荐、最热、即将截止、最新。
- 详情访问时按用户会话或自然日去重累加浏览量，并聚合真实收藏量用于最热排序。
- 筛选状态继续写入 URL，返回列表时保留筛选和滚动位置。

### 9. 将竞赛详情改为结构化、可维护的数据

当前状态：

- 当前详情已有主办方、对象、截止时间、适合人群、行动建议、关联资料和相关组队。
- 官网地址目前混在 `actionHints` 文本中，赛制、附件和更新时间没有独立字段。

建议修改：

- 增加结构化字段：报名时间、比赛时间、参赛对象、团队人数、赛事级别、赛制阶段、奖项设置、费用说明、官方联系方式、来源链接、最后核验时间。
- 增加官方通知和附件表，保存标题、发布日期、来源地址、文件类型和本地/对象存储地址。
- 详情按“基本信息、赛程阶段、参赛说明、官方通知、相关组队、资料、经验帖”展示。
- 来源链接必须可以直接点击；展示“最后核验时间”和“以官网为准”。
- 继续使用真实官网信息整理，不把官网整页截图作为主要数据来源。

## P2：内容沉淀与本地开发

### 10. 建立轻量问答闭环

建议修改：

- 复用现有社区“问答”分类，增加“最新、待回答、已解决”筛选。
- 提问时可关联真实竞赛，并选择报名、组队、材料、赛制等标签。
- 发帖人可以采纳一条回答，列表显示已解决状态。
- 认证答主标识必须来自真实审核记录，不允许用静态文案伪造。

### 11. 增加本地一键预览环境

建议修改：

- 增加一个本地命令同时启动 API 和前端，默认使用本地 SQLite、文件存储和模拟微信登录。
- 提供两个学校的最小测试数据和管理员账号初始化脚本。
- 明确区分本地 `.env.local` 与生产环境，不依赖已过期服务器。
- 本地验收固定覆盖 390x844、414x896 和管理端桌面尺寸。

## 推荐实施顺序

1. 学校认证访问门槛与内容范围模型。
2. 学校后台权限和导航收敛。
3. 学校隔离自动化测试。
4. 首页补齐本校组队和经验内容。
5. 结构化组队发布、列表和详情。
6. 竞赛筛选与详情数据结构。
7. “求加入”模式和轻量问答。
8. 本地一键预览与持续回归。
