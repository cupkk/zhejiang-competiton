# 校园成长平台研究进展总览

## 项目目标

- 当前优先级仅为微信小程序用户端，不继续扩展管理员界面。
- 首页保持紧凑搜索与竞赛、资源、组队三块内容预览。
- 用户看到的竞赛和资源事实必须可由主办方官网、官方通知或官方 PDF 核验。
- 组队内测数据必须明确标注、无虚假联系方式；浙江大学展示示例可跨校浏览，真实内容由发布者选择仅本校或全部高校。

## 已完成基线

- 用户端核心浏览链路已完成一轮 Emil / Apple 风格整理，搜索支持原位展开、聚焦、清除和取消。
- 公开接口当前固定返回 30 项竞赛，3 份平台原创可下载资料。
- 浙江大学、北京大学、中国石油大学（北京）已有各 3 条同校内测组队示例。
- 上一轮本地 release 校验和 390 / 414 / 430 px 页面检查均通过。

## 当前关键问题

- 30 项竞赛虽然结构字段齐全，但写入脚本仍从数据库已有空日期推导赛程，导致全部显示 `长期规则信息`、`not_announced` 和 `待发布`。
- 多项赛事来源仍错误指向中国高等教育学会竞赛目录，不是主办方赛事页面。
- 内容校验只检查字段是否存在，未检查届次、日期、通知来源和状态之间是否一致。
- 组队示例只预置 3 所学校，其他已认证学校仍可能看到空组队大厅。
- 数据库还有若干“官网入口”类资源，部分来源不是实际主办方官网，应清理或转入审核。

## 本轮执行原则

- 赛氪仅用于发现线索和比较信息架构，不作为最终事实来源，不复制其文案或视觉资产。
- 2026 届未发布时保留最新可核验届次，并明确标注；不猜测日期、电话、邮箱或费用。
- 赛事质量状态由证据决定，不能因为字段有模板文字就标记为 `verified`。
- 所有生产数据脚本继续默认 dry-run，PostgreSQL 写入必须显式提供 `--apply --confirm-production`。

# 2026-07-27 工作记录

## 09:00 续接与审计

- 阅读 `experiment journal 20260726.md`、当前工作区状态、`scripts/enrich-official-content.ts`、内容校验脚本和组队可见性逻辑。
- 确认上一轮视觉与基础闭环已经完成，本轮先处理真实内容和跨学校空状态，再做视觉复核。
- 确认工作区包含大量历史修改和验收产物，本轮不清理、不重置、不覆盖无关改动。

## 当前计划

1. 建立 30 项赛事证据表。
2. 扩展赛事种子字段并修复状态推导。
3. 加强公开内容质量校验。
4. 实现按学校惰性生成内测组队示例。
5. 补充原创资料，清理错误“官网入口”资源。
6. 运行完整本地闭环和多视口验收。

## 10:00 竞赛证据与状态修复

- 新增 `content/competition-evidence-20260727.md`，逐项记录 30 项竞赛的届次、当前阶段、官方来源和证据等级。
- `scripts/enrich-official-content.ts` 增加显式证据字段：届次、赛程状态、当前阶段、通知标题、通知地址、通知日期、已核验日期和可选精确赛程。
- 不再读取数据库已有空日期推导全部赛事状态。
- 已写入数学建模 2026 报名截止、竞赛时间和官方联系方式；电子设计专题赛、蓝桥杯、智能车和米兰设计周写入已核验日期。
- 智能车、机械创新、机器人等错误或泛化入口改为赛事主办方官网。
- 删除公开内容中的“见官网”“以当届通知为准”等模板句；未公开费用、奖项比例和联系方式时字段为空，前端不显示该模块。
- `server/db.ts` 修复启动迁移回归：已核验竞赛不再被启动过程改写状态，也不再重新填入模板费用或奖项。
- 推荐排序改为进行中 / 已发布优先，待发布时间其次，已结束最后；“即将截止”不再把负数日期排在首位。

## 11:00 任意学校组队示例

- 新增 `server/team-example-templates.ts`，三条内测示例只有一个模板来源。
- 新增 `server/team-example-service.ts`，用户进入首页、组队大厅或竞赛组队时，为其已认证学校按需创建示例。
- 每校最多 3 条，45 天过期；真实有效组队达到 3 条后沿用既有逻辑自动隐藏。
- 示例仅同校已认证用户可见，详情跨校访问被拒绝，联系方式固定不可获取。
- 复旦大学作为未预置示例学校完成验证：首次访问生成 3 条；浙江与复旦互访详情均为禁止。

## 12:00 资源真实性与本地预览

- 新增 3 份平台原创 Markdown 资料：
  - `content/resources/2026数学建模参赛检查清单.md`
  - `content/resources/软件类竞赛作品交付清单.md`
  - `content/resources/组队联系与信息安全清单.md`
- 平台原创可下载资料由 3 份增至 6 份。
- 无文件的历史 mock 资源、动态 P3 测试资源和错误通用入口转入待审核或在本地种子阶段清理，不再出现在用户资源列表。
- 首页空运营配置默认选择数学建模 2026、创新大赛、数学建模检查清单和竞赛核验清单。
- 学校首页优先展示明确标注的同校内测组队示例，不再让闭环测试组队占据首屏。
- 新增 `scripts/seed-local.ts`；`npm run seed:local` 现在保证双学校数据和官方增强都写入 `campus-growth-local-preview.db`，修复第二段脚本误写默认 SQLite 的问题。
- 本地双学校隔离夹具保留用于自动化测试，标题统一标注“内测”“预览”或“闭环测试”；生产内容脚本不创建这些夹具。

## 13:00 前端状态显示

- 竞赛卡片、首页和详情依据 `scheduleStatus` 区分“时间待发布”“赛程进行中”“日程已发布”和“本届已结束”。
- 修复已结束但没有精确报名日期时误显示“本届时间待发布”。
- 详情报名时间和比赛时间缺失时按状态显示“详见官方赛程”或“本届具体日期未收录”，不编造日期。
- 搜索结果同步使用同一状态逻辑。

## 14:00 验证结果

- `npm run verify:local-release`：通过。
  - 根目录 TypeScript lint：通过。
  - 前端 typecheck：通过。
  - 内部路由安全：通过。
  - 用户 / 管理员学校隔离、组队发布、审核、举报、审计闭环：通过。
  - 管理员内容发布生命周期、上传文件魔数、限流和安全头：通过。
  - Vite 生产构建：通过。
- `scripts/verify-public-content.ts`：30 项公开竞赛、6 份原创资源、`incomplete=0`。
- 模板文案扫描：`关注官网 / 见官网 / 以当届 / 长期规则信息` 命中 0。
- API 重启回归：智能车仍保持“赛区赛阶段”，空费用和奖项没有被迁移脚本重新填充。
- Playwright 真实浏览器检查：
  - 页面：首页、竞赛列表、数学建模详情、资源列表、数学建模资料详情、组队大厅、示例详情。
  - 视口：390x844、414x896、430x932。
  - 21 组页面全部 `scrollWidth = clientWidth`，越界元素 0，交互目标小于 44 px 为 0。
  - 控制台错误 0。
  - 首页搜索展开后自动聚焦，回车进入 `/search?keyword=数学建模&scope=all`。
  - 数学建模详情出现 `2026 高教社杯` 与 `2026-09-07`；历史 P3 和旧 mock 标题未出现。
- 最终截图：
  - `output/playwright/user-home-390-20260727.png`
  - `output/playwright/user-competitions-390-20260727.png`
  - `output/playwright/user-competition-mcm-390-20260727.png`
  - `output/playwright/user-resources-390-20260727.png`
  - `output/playwright/user-resource-mcm-390-20260727.png`
  - `output/playwright/user-teams-390-20260727.png`
  - `output/playwright/user-team-example-390-20260727.png`
  - `output/playwright/user-home-search-expanded-390-20260727.png`

## 当前边界与下一步

- 本轮按用户要求只完善小程序用户端和本地预览；未更新管理员 UI。
- 本轮未连接已过期服务器、未部署生产、未上传微信开发版本。
- 30 项竞赛的结构和当前届次均有官方入口证据；证据表中 C 级赛事仍需在主办方发布可读取的正式通知后补充精确日期，不使用第三方日期代替。
- 下一步优先做真机微信壳回归：登录、首页、竞赛收藏、资源领取下载、组队发布、举报和滚动返回记忆；通过后再决定是否生成新的微信开发版本。

## 15:00 新一轮产品差距审计与规划

### 用户反馈

- 用户仍感知不到足够的组队模板。
- 部分竞赛详情仍像“见官网”，希望本届未发布时直接展示上一届可核验规则。
- 希望调查可持续的数据接口，不再只靠一次性人工录入。
- 当前白卡片、浅灰背景和蓝色按钮组合过于朴素，需要整体更换视觉风格。

### 现场审计

- 浙江大学当前可见 4 条组队：2 条“找队伍”示例、1 条“求加入”示例、1 条本地闭环测试数据。模板存在，但数量少且被两个标签页拆开，发现性不足。
- 30 项公开竞赛中只有物流设计仍命中“待发布”文本。
- 29 项至少缺少报名截止、比赛时间、联系方式、费用、奖项中的 3 类；这些字段不一定都由主办方公开，但详情页因此大量使用泛化回退文案。
- 证据表仍有 6 项 C 级赛事：物流设计、中国高校计算机大赛、结构设计、三创赛、市场调查与分析、物联网设计。
- 当前视觉主要依赖 `#f4f6f9` 背景、白色 8 px 卡片、slate 文本和 Apple 蓝按钮；首页三个模块虽有色彩区分，但列表与详情仍高度同构，缺乏赛事状态、内容类型和学校身份形成的层级。

### 决策

1. 不使用赛氪未公开接口作为生产数据源；它只作为线索来源。优先寻找主办方公开 JSON、HTML、RSS、下载附件和官方 PDF。
2. 为每项竞赛保存“当前届次”和“参考届次”。当前届次未发布时，页面明确显示“2026 尚未发布，以下参考 2025”，并展示完整的上一届规则及来源。
3. 抓取更新只生成差异草稿，人工审核后发布；不得自动覆盖已发布记录。
4. 组队同时提供“大厅示例”和“发布模板”：扩展为 6 个典型方向，仍明确标注、同校隔离、无虚假联系方式。
5. 新视觉采用“iOS 18 Grouped Utility + 支付宝校园服务信息密度”，不采用营销页、渐变装饰、超大圆角或卡片墙。
6. 先完成首页、竞赛列表/详情、组队大厅/发布页三组样板；通过截图审查后再覆盖资源、社区、搜索、个人页和登录引导。

### 下一阶段验收

- 组队大厅默认可看到不少于 4 条“找队伍”和 2 条“求加入”模板，发布页可一键套用后编辑。
- 30 项竞赛全部有明确当前/参考届次；用户可见字段中“见官网”“以通知为准”命中 0。
- 当前届次未发布的赛事至少展示上一届团队人数、阶段、材料、报名方式和官方来源。
- 数据抓取有来源、哈希、抓取时间、差异和审核状态，不直接发布。
- 新样板在 390 / 414 / 430 px 下无横向滚动，所有触控目标不小于 44 px，固定底栏不遮挡末项。

## 16:00 六套组队模板与发布复用

- 新增共享模板源 `frontend/src/data/team-templates.ts`，服务端 `server/team-example-templates.ts` 直接复用，避免大厅示例和发布模板各自维护后出现内容漂移。
- “找队伍”扩展为 4 套：创新大赛视觉/产品、数学建模编程/论文、电子设计嵌入式/硬件、挑战杯调研数据/写作。
- “求加入”扩展为 2 套：计算机设计前端/视觉、软件项目后端/测试/文档。
- `PublishTeam.tsx` 增加“快速套用”；模板只填写竞赛、标题、简介、角色、能力、目标、合作方式和时间投入，联系方式仍为空且必须由发布者本人填写。
- 模板入口最终采用两列自适应网格。此前横向轮播会产生屏外元素，已删除该方案。
- 浙江大学本地 API 当前可见 4 条招募示例、2 条求加入示例；示例继续明确标注“内测示例”，只在同校展示，不提供虚假联系方式。

## 16:30 竞赛届次与官方源监控

- 竞赛增加 `currentEditionLabel`、`referenceEditionLabel`、`referenceNoticeUrl`、`scheduleNote` 和 `dataFreshness`。
- 当前届次未发布时，详情顶部统一说明“2026 届尚未发布”，正文展示最近一届可核验规则及官方来源，不再用“见官网”代替关键信息。
- 已补充结构设计、三创赛、市场调查、物联网设计等赛事的官方日程或通知；物流设计展示第九届（2025）参考规则。
- 新增 `scripts/data/official-competition-sources.ts` 和 `scripts/sync-official-competition-sources.ts`，登记 30 个主办方控制的来源。
- 官方源同步仅保存 HTTP 状态、内容哈希、抓取时间和检测到的届次/日期；新哈希进入 `pending_review`，不会直接覆盖或发布竞赛。
- 赛氪仅作为发现线索，不作为生产事实源。PostgreSQL 写入仍要求 `--apply --confirm-production`。
- `content/competition-evidence-20260727.md` 已同步更新；中国高校计算机大赛仍是唯一 C 级证据项，等待主办方发布可稳定读取的正式通知。

## 17:00 核心视觉 Before / After 审查

视觉方向确定为“iOS 18 Grouped Utility + 支付宝校园服务信息密度”，只覆盖本轮用户端核心链路。

| 检查项 | Before | After |
| --- | --- | --- |
| 首页层级 | 白卡片和平铺列表同构，模块辨识度低 | 蓝/绿/橙三组内容区，分别承载竞赛、资源、组队预览 |
| 搜索 | 长条搜索框持续占据顶部 | 约 96 px 搜索胶囊，点击原位展开、自动聚焦、支持清除和取消 |
| 竞赛列表 | 标题和标签为主，当前状态不突出 | 状态色轨、届次、截止、队伍规则、浏览和核验日期形成扫描层级 |
| 竞赛详情 | 普通标题卡，参考年份来源不清 | Apple 蓝赛事通行证头部；参考届次使用琥珀色来源说明 |
| 组队大厅 | 示例发现性弱，找队伍/求加入层级接近 | 橙/蓝状态轨、学校统计、角色和目标信息分层 |
| 发布组队 | 长表单缺少起点 | 先选发布类型和模板，再编辑结构化字段 |
| 底部导航 | 普通白底固定栏 | 轻量半透明材质、明确选中态和安全区适配 |
| 动效与反馈 | 部分动作无统一节奏 | 按压 100-140 ms；搜索展开 180 ms；减少动态模式立即切换 |

- 材质层级保持克制：卡片圆角 8 px，搜索胶囊和圆形工具按钮例外；没有装饰渐变、漂浮图形和无意义动画。
- 最终人工复核首页、竞赛列表、参考届次详情、组队大厅和发布页，未发现文字重叠、颜色突兀或卡片套卡片。

## 18:00 最终移动端回归与修复

### 发现并修复

- `Layout.tsx` 原先对新页面也在 80-2600 ms 内反复执行 `scrollTo(0, 0)`。用户进入页面后立即滑动会被拉回顶部。
- 修复为：新页面只在首次进入时置顶；只有浏览器返回且目标滚动位置大于 0 时才安排延迟恢复。
- `index.css` 的搜索胶囊入场动画从 `scale(0.985)` 改为 `translateY(1px)`，避免动画期间 44 px 触控目标被缩到约 43.34 px。

### Playwright 结果

- 视口：390x844、414x896、430x932。
- 路由：首页、竞赛列表、数学建模详情、物流设计参考届次详情、找队伍、求加入、发布招募、发布求队，共 24 组。
- 24 组全部满足：
  - `scrollWidth = clientWidth`，横向越界元素 0。
  - 小于 44 px 的可见交互目标 0。
  - 控制台错误和页面异常 0。
  - 首页、竞赛、组队末项与固定底栏间距为 43-55 px，未被遮挡。
- 发布模板：招募 4 个、求加入 2 个；点击后标题正确填入，联系方式保持空值。
- 滚动记忆：详情首次进入 `scrollY=0`；返回列表恢复原位置，实测误差 `0`；切换到新页面 `scrollY=0`。
- 最终截图位于 `output/playwright/final-*-20260727.png`，覆盖上述 8 个页面和 3 个视口。

## 19:00 发布前本地校验

- `npm run verify:local-release`：通过。
  - TypeScript lint、前端 typecheck、内部路由安全、用户/管理员学校隔离、组队发布、审核、举报、内容发布生命周期、上传文件魔数、限流、安全头和前端生产构建全部通过。
- `npx tsx scripts/verify-public-content.ts`：30 项公开竞赛、6 份资源、`incomplete=0`。
- `npx tsx scripts/sync-official-competition-sources.ts --limit=3`：dry-run 3/3 成功，三个官方来源均返回 HTTP 200。

## 当前交付边界（部署前）

- 本轮小程序用户端需求和本地预览验收已经完成，管理员 UI 未扩展。
- 当时服务器仍处于过期状态，因此该阶段没有部署生产，也没有上传新的微信开发版本；此状态已被下方 20:00 发布记录取代。
- 本地前端和 API 保持运行：
  - `http://127.0.0.1:3001/`
  - `http://127.0.0.1:8080/api/health`
- 正式上线前仍需完成真实 AppSecret 轮换、邮件/短信服务配置、可用服务器和微信真机完整闭环。

## 20:00 生产发布与微信 0.1.20

### 发布前审计

- 服务器恢复后重新核验：
  - `campusgrow.top` 与 `www.campusgrow.top` 均解析到 `121.43.58.9`。
  - HTTP 301 跳转 HTTPS；HTTPS 200，HSTS、CSP、nosniff、SAMEORIGIN、Referrer-Policy 和 Permissions-Policy 保持有效。
  - 发布前生产 release 为 `20260726140538`，API 为 `postgres + s3 + real WeChat + payments disabled`。
  - 根分区剩余约 32 GB；Nginx 配置检查通过；`pg_dump`、`createdb` 和生产环境文件可用。
- 发现 `scripts/build-release-package.ps1` 未包含新共享依赖 `frontend/src/data/team-templates.ts`。已补入白名单并验证归档包含该文件，避免重现 `ERR_MODULE_NOT_FOUND`。
- `wechat-shell/pages/webview/index.js` 的 `SHELL_BUILD` 从 `0.1.19` 更新为 `0.1.20`。
- `npm run verify:local-release` 通过；根生产依赖 0 漏洞。
- 前端依赖审计仍只有已记录的 React Router RSC 模式公告。当前为 React 18 + Vite SPA，不启用 RSC/action 路径，沿用既有例外，不执行破坏性强制降级。

### 备份与发布

- 发布包：`.deploy/campus-growth-20260727150903.tar.gz`。
  - 96 个条目，1,999,791 字节。
  - 敏感文件命中 0。
  - SHA256：`19fbdf043de4caee5e03b0dd6906380af1a2cb3b6a2b277cb44f98f457326792`。
  - 本地与服务器 SHA256 一致。
- 生产备份：`predeploy-ui-20260727-20260727150939.sql.gz`。
  - 127,718 字节。
  - SHA256：`05c2dd7ba300d4d90239c3cdbecd9832d9175e56fd6b252f4f96761f762df873`。
  - 已恢复到临时 PostgreSQL：38 张公开表；核心计数 `2995|4|85|36|12|9|1`；恢复检查通过，临时库已删除。
- `deploy-commercial-release.sh` 原子切换到 `/opt/campus-growth/releases/20260727150903`，服务健康检查通过，未触发回滚。
- 内容增强脚本首次通过 sudo shell 调用时误用了 `/usr/bin/node v20`，报 `node:sqlite` 不存在；确认 systemd 使用 `/usr/local/bin/node v24.14.1` 后，改用相同运行时执行成功。
- 生产写入结果：
  - 30 项 `published + verified` 竞赛。
  - 6 份审核通过的免费资源。
  - 浙江大学、北京大学、中国石油大学（北京）各 4 条招募示例和 2 条求加入示例。
  - 物流设计字段为“2026 届尚未发布 / 参考第九届（2025） / reference”。

### 线上验收

- 外网与服务器本机健康检查均返回新 release：`20260727150903`。
- 服务状态为 `active`，发布完成后的 systemd error 日志为空。
- 生产 Playwright 检查：首页、竞赛列表、物流设计详情、资源、组队、登录。
  - 390x844 下所有页面 `scrollWidth = clientWidth`。
  - 控制台错误 0。
  - “见官网 / 以通知为准”命中 0。
- 截图：
  - `output/playwright/production-home-390-20260727.png`
  - `output/playwright/production-reference-390-20260727.png`

### 微信开发版本

- 微信开发者工具 CLI 登录状态为 true，AppID 校验为 `wxda8641cd650537a4`。
- preview 成功，二维码：`output/wechat-preview/release-0.1.20-20260727150903.png`。
- upload 成功，开发版本：`0.1.20`，描述“核心界面、竞赛内容与组队模板更新 20260727”。
- 包体 3,047 字节；信息文件：
  - `output/wechat-preview/release-0.1.20-20260727150903.json`
  - `output/wechat-preview/upload-0.1.20-20260727150903.json`
- CLI 不能自动设为体验版。仍需在微信公众平台手动将 `0.1.20` 设为体验版后进行真机闭环。

### 清理与当前边界

- 服务器 `/tmp` 中本次发布包、部署脚本、备份脚本、内容脚本和验收脚本均已删除。
- 生产 release、上一 release、数据库备份、本地白名单归档、二维码、上传信息和验收截图保留。
- 本次完成的是生产部署和微信开发版上传，不等同于微信正式审核发布。
- 正式审核前仍需确认 AppSecret 已轮换、真实邮件/短信可发送，并完成 `0.1.20` 真机完整闭环。

## 21:00 加载失败稳定性热修复

### 风险判断

- 当前最没有把握的部分是真实微信 WebView 在弱网、网络切换、旧缓存和微信登录 code 时效下的稳定性。浏览器自动化不能替代真机长期运行。
- 用户此前没有充分意识到的最大遗漏有两层：
  1. 缺少接口失败率、P95 延迟、客户端 JS 错误和用户网络类型等持续可观测性，出现问题后只能依赖人工反馈。
  2. 生产数据库适配器在 Node 主线程中同步等待单个 PostgreSQL worker，HTTP 请求会排队；这不是当前小规模内测的硬阻塞，但并发继续增加后会成为容量上限。

### 现场证据

- systemd 没有业务 API error 日志；Nginx error 日志中的 TLS 握手失败来自外部扫描地址，与页面加载无关。
- 最近 5000 条 Nginx 请求以 200/301 为主，仅有 1 条 499，没有持续 5xx。
- 热修复前 30 并发外网压测：
  - `/api/resources`：30/30 成功，P50 约 2036 ms，P95 约 3936 ms。
  - `/api/competitions`：30/30 成功，P95 约 811 ms。
- 资源列表每条资源会分别查询关联赛事和文件；登录用户还会逐条查询收藏、领取和待支付状态，形成明显 N+1。
- 前端 `fetch` 没有超时、重试和同请求合并；缓存过期会立即删除，网络失败时无法继续显示旧内容。

### 修复

- `server/catalog-service.ts`
  - 增加资源批量映射上下文。
  - 关联赛事、资源文件、收藏、已领取和待支付状态改为批量查询。
  - 首页资源、竞赛关联资源、资源列表和我的投稿统一使用批量映射。
  - 匿名资源列表数据库往返从约 13 次降为固定 3 次；登录用户从逐条查询降为固定 6 次以内。
- `frontend/src/app/lib/http.ts`
  - GET 默认 12 秒超时，普通写操作 20 秒，文件上传 60 秒。
  - GET 网络错误和 408/425/429/5xx 最多重试 2 次，退避 250 ms / 700 ms。
  - 相同 URL 和登录态的并发 GET 合并为同一个 Promise。
  - 超时和断网返回简短、可操作的错误提示。
- `frontend/src/app/lib/query-cache.ts`
  - 缓存命名空间升级为 `v2`，避免旧结构污染。
  - 过期缓存不再立即删除，只在网络失败时作为兜底。
- `frontend/src/app/hooks/useRequestState.ts`
  - 刷新失败且存在旧缓存时保持现有内容，不再切换到整页“加载失败”。
- 微信壳 `SHELL_BUILD` 更新为 `0.1.21`。

### 验证

- `npm run verify:local-release` 全部通过。
- Playwright 主动中断前两次竞赛请求：
  - 第 3 次请求成功，页面自动恢复。
  - 重试总数为 3，没有出现加载失败状态。
- Playwright 让三次请求全部失败：
  - 过期缓存成功兜底。
  - 页面继续显示真实内容，无横向溢出。
- 独立本地 API 30 并发：
  - 资源列表 30/30 成功，P95 约 27 ms。
- 热修复后生产 30 并发：
  - 资源列表 30/30 成功，P50 约 521 ms，P95 约 1005 ms，较修复前 P95 降低约 74%。
  - 生产健康检查 30/30 成功。
- 生产弱网回归再次通过：前两次失败后自动恢复；持续失败时旧缓存兜底；截图 `output/playwright/production-resources-recovered-390-20260727.png`。

### 发布

- 生产备份：`predeploy-loading-hotfix-20260727153257.sql.gz`。
  - 129,496 字节。
  - SHA256：`a00e4d5a1af0e4e03f68a058f2e6502b0961875bfb667c47cdf95f489200ab63`。
  - 已恢复到临时 PostgreSQL，39 张公开表，恢复检查通过。
- 发布包：`.deploy/campus-growth-20260727153213.tar.gz`。
  - SHA256：`588d772ee57147be33020db02263e04c2640ab11103c739067b4f52ff06a36d1`。
  - 敏感文件命中 0，本地与服务器哈希一致。
- 当前生产 release：`/opt/campus-growth/releases/20260727153213`。
- 服务状态 active，发布后 error 日志为空。
- 微信 preview/upload 成功：
  - 开发版本：`0.1.21`。
  - 二维码：`output/wechat-preview/reliability-hotfix-0.1.21-20260727153213.png`。
  - 描述：“修复页面加载失败与资源接口性能 20260727”。

### 后续硬任务

- 接入前端错误采集、API 请求耗时、Nginx request_time 和告警，才能量化“经常加载失败”是否彻底消失。
- 将同步 PostgreSQL 适配层改为异步调用，或先以多进程 API + Nginx upstream 作为过渡扩容。
- 将 `0.1.21` 手动设为体验版，在真实微信中进行 4G/Wi-Fi 切换、后台恢复和弱网重试测试。

## 22:00 浙江大学组队展示模式

### 需求判断与边界

- 用户要求当前阶段只展示浙江大学组队案例，删除其他学校案例，并允许不认证学校直接查看组队效果。
- 没有删除 `schools`、`user_school_memberships` 或真实学校隔离模型。直接删除这些结构会破坏后续多校运营。
- 新增“展示与业务分离”路径：
  - 匿名、未认证和其他学校用户可以查看明确标注的浙江大学示例。
  - 默认业务接口继续执行学校隔离。
  - 真实组队、真实联系方式、发布、申请和审核仍要求登录及原有权限。

### 实现

- `TeamQuery` 增加 `showcase`。
- 用户端组队大厅默认请求 `showcase=true`：
  - “找队伍”固定展示 4 条浙江大学招募案例。
  - “求加入”固定展示 2 条浙江大学个人案例。
  - 页面标识改为“浙江大学案例 / 组队招募展示”。
  - 展示模式下不出现学校认证提示。
- 浙江大学示例详情允许匿名查看；示例联系方式始终为空，接口仍拒绝联系方式查看。
- 展示模式下进入 `/teams` 和示例详情不会被未完成的新用户引导层遮挡。
- 默认 `/api/teams` 不向匿名用户返回真实学校内容；展示数据必须显式使用 `showcase=true`。
- `server/team-example-service.ts` 停止为非浙江大学学校自动生成示例。
- `scripts/enrich-official-content.ts` 的示例学校清单缩减为浙江大学。
- 新增 `scripts/configure-team-showcase.ts`：
  - 默认 dry-run。
  - PostgreSQL 写入要求 `--apply --confirm-production`。
  - 只删除 `is_example=1` 的示例和相关依赖，不删除真实组队、学校或用户。
  - 删除后按共享模板重建浙江大学 6 条案例。

### 本地与管理员验证

- 本地数据清理前：浙江大学、复旦大学、北京大学、中国石油大学（北京）各 6 条示例。
- 清理后：仅浙江大学 `team_recruit=4`、`member_available=2`。
- `npm run verify:local-release` 全部通过。
- 用户端 Playwright：
  - 390/414/430 px 下找队伍均为 4 条、求加入均为 2 条。
  - 全部案例 `schoolId=sch_114` 且 `isExample=true`。
  - 无认证提示、无横向溢出、控制台错误 0。
  - 匿名详情可打开；真实联系方式不展示；安全提示保留。
- 管理员端：
  - 平台管理员示例列表恰好 6 条，均为浙江大学。
  - 学校筛选、全选使用中、批量归档可用。
  - 桌面 1440 px 和移动 390 px 无横向溢出。
  - 截图：
    - `output/playwright/admin-team-showcase-1440-20260727.png`
    - `output/playwright/admin-team-showcase-390-20260727.png`

### 生产发布

- 生产备份：`predeploy-zju-showcase-20260727162441.sql.gz`。
  - 130,415 字节。
  - SHA256：`3d4192cdf30a4b406a1cfd886dceb961c6ab60454dbfde62561f5525f9c197c2`。
  - 已恢复到临时 PostgreSQL，39 张公开表，恢复检查通过。
- 发布包：`.deploy/campus-growth-20260727162402.tar.gz`。
  - 97 个条目，敏感文件命中 0。
  - SHA256：`40a001128c5d9dc4f2f0ec898b9405ce966bef715e1ec3d8bfbd9c39d96b709a`。
- 当前生产 release：`/opt/campus-growth/releases/20260727162402`。
- 生产数据脚本执行前：浙江大学、北京大学、中国石油大学（北京）各 6 条示例。
- 执行后：仅浙江大学 4 条招募、2 条求加入。
- 生产匿名 API 和 Playwright 均通过：
  - `/api/teams?showcase=true&listingType=team_recruit` 返回 4。
  - `/api/teams?showcase=true&listingType=member_available` 返回 2。
  - 默认匿名 `/api/teams` 返回 0。
  - 页面无认证提示、横向溢出或控制台错误。
- 微信开发版本 `0.1.22` preview/upload 成功：
  - 二维码：`output/wechat-preview/zju-team-showcase-0.1.22-20260727162402.png`。
  - 描述：“浙江大学组队展示模式与匿名预览 20260727”。

### 当前需求完成度

- 小程序端核心体验：已完成本轮范围，包括登录、首页、竞赛、资源、社区、组队、个人资料、滚动记忆、弱网恢复和移动端适配。
- 管理员端功能闭环：已具备平台/学校管理员权限、学校筛选、内容审核、批量处理、举报、竞赛和资源人工发布、学校管理、首页配置、示例归档和审计日志。
- 尚未完成或不能宣称完成：
  - 真实邮件/短信发送服务。
  - AppSecret 已轮换的外部平台确认。
  - 微信 `0.1.22` 手动设为体验版后的真机完整闭环。
  - 客户端错误采集、API P95、告警和容量监控。
  - PostgreSQL 同步适配层异步化或多进程扩容。
  - 管理员端所有页面逐页视觉精修；目前是功能完整、关键页面可用，不是最终视觉版本。
## 21:35 跨校组队可见范围与邮箱直联闭环

### 本轮决策

- 将“内容所属学校”“可见范围”“联系渠道”拆分：组队内容始终归属发布者当前学校，发布者可选择“仅本校”或“全部高校”，默认仅本校。
- 平台不提供站内私聊、申请加入或撮合。队长必须填写联系邮箱，感兴趣的用户通过系统邮件客户端直接联系；社区帖子和评论继续保留。
- 浙江大学内测示例继续跨校展示，但明确标记“内测示例”，不填写或伪造联系邮箱。

### 已完成

- 数据库与接口增加 `visibility_scope`、`contact_email`，列表支持 `schoolScope=all|current|other`。
- 组队列表增加“全部高校 / 本校 / 其他学校”筛选；卡片展示所属学校和“跨校公开 / 仅本校”。
- 发布页增加可见范围选择和必填邮箱校验；详情页增加复制邮箱、`mailto:` 和统一安全提示。
- 默认关闭 `TEAM_APPLICATIONS_ENABLED`；旧申请接口返回 `team_applications_disabled`，前端删除队伍申请页、路由和“我的申请”入口。
- “我的发布”只返回本人发布内容；旧申请记录保留在数据库中用于历史审计，不再进入用户主路径。
- 后台组队审核任务增加可见范围和联系邮箱，管理员可以在公开前检查是否误填敏感信息。
- 本地双校固定数据显式写入学校范围和测试邮箱；浙江大学展示示例保持 4 条招募、2 条求加入。

### 验证结果

- `npm run verify:local-release` 通过：lint、前端类型检查、内部路由安全、用户/管理员学校矩阵、内容安全和前端生产构建均通过。
- 本地闭环验证：浙大用户发布跨校组队 -> 浙大管理员审核 -> 复旦用户可从“其他学校”查看详情和邮箱；复旦“本校”列表不会混入该内容。
- SQLite 持久化断言通过：`visibility_scope=cross_school`，`contact_email=team-smoke@example.com`。
- Playwright 检查 `/teams`、组队详情、`/publish-team`，覆盖 390x844、414x896、430x932；横向溢出为 0，邮箱输入聚焦后字号 16px、`visualViewport.scale=1`，控制台错误 0。
- 截图：`output/playwright/teams-cross-school-390-20260727.png`。

### 待完成

- 真实邮件发送服务仍未接入；当前邮箱联系依赖用户设备邮件客户端，符合本阶段“不在平台内撮合”的产品边界。

### 生产发布与微信上传

- 完整发布验证在后台审核元数据补齐后再次通过。
- 生产备份：`predeploy-cross-school-email-20260727214105.sql.gz`。
  - 129,584 字节。
  - SHA256：`c2ca3f0984e15844ce739b1188160ec9011338db79f0b37c75963e00f028d354`。
  - 已恢复到临时 PostgreSQL，39 张公开表，恢复检查通过。
- 发布包：`.deploy/campus-growth-20260727213924.tar.gz`。
  - 97 个条目，敏感文件命中 0。
  - SHA256：`a945ba998a53acdcb01e4984b647fe3c18253cd19b20e4088bdb144befd30f0d`，本地与服务器一致。
- 当前生产 release：`/opt/campus-growth/releases/20260727213924`；服务 active，健康检查返回 PostgreSQL、S3、真实微信登录、支付关闭、站内组队申请关闭。
- 生产浙江大学示例刷新后仍为 4 条招募、2 条求加入，全部 `cross_school`，联系邮箱数量为 0；默认匿名真实队伍列表为 0。
- 生产 Playwright 验证组队列表和详情无横向溢出、无控制台错误、无空分隔符和虚假邮箱。
- 微信 `0.1.23` preview/upload 成功，包体 3,047 字节；二维码：`output/wechat-preview/cross-school-email-0.1.23-20260727213924.png`。
- 仍需在微信公众平台手动将 `0.1.23` 设为体验版，并使用真机验证登录、跨校筛选、发布、邮件客户端唤起和后台审核。

## 22:55 上线可用性审计、首页修复与内容巡检

### 风险判断

- 无法仅凭桌面自动化证明微信真机 `web-view + wx.login + 系统邮件客户端` 在所有机型稳定，因此不承诺“零 bug”。
- 最大遗漏是缺少客户端错误观测和持续内容巡检；健康接口正常时，仍可能发生页面白屏、登录循环或内容过期。

### 修复与验证

- 修复首次访客首页组队空态：首页现在展示可跨校浏览的浙江大学示例，已认证学校配置仍优先展示本校推荐。
- 首页模块数量原为预览数组长度，容易让用户误以为平台只有 2 项内容，已改为“精选”。
- 新增客户端错误上报：只发送错误类型、页面路径、技术消息和截断堆栈，不发送昵称、邮箱、手机号或表单正文。
- 新增 API 慢请求/5xx 日志：耗时超过 2 秒或状态码 5xx 时记录方法、路径、状态和耗时。
- 生产错误上报实测返回 204，并在 systemd 日志中出现脱敏事件。
- Playwright 覆盖首页、竞赛列表/详情、资源列表/详情、组队列表/详情、社区、个人和登录，检查 390x844、414x896、430x932：横向溢出 0、可见触控目标均不小于 44px、控制台错误 0。
- 生产真实闭环通过：临时用户发帖、评论、组队发布、管理员审核、邮箱披露、站内申请禁用、资源上传/审核/收藏/免费领取、举报处理和通知；临时数据已清理。
- 生产 15 次串行请求结果全部成功：健康 P95 146ms，首页 P95 444ms，竞赛 P95 135ms，资源 P95 145ms，组队 P95 137ms，帖子 P95 105ms。
- npm 官方审计：根目录生产依赖 0 漏洞；前端仍有 React Router RSC 模式公告，本项目为 Vite SPA、不使用 RSC，保留已记录例外。

### 内容巡检

- 修复资讯抓取覆盖已发布正文的问题：内容未变化不处理，发生变化时重新设为 pending 并创建审核任务。
- 生产写入新增 `--confirm-production` 硬保护。
- 安装 `campus-growth-content-monitor.timer`，每日 04:20 起随机延迟最多 20 分钟执行。
- 首次执行发现并修复 PostgreSQL worker 未退出导致 oneshot 挂起的问题。
- 修复后手动执行成功，`Result=success`：30 个官方来源完成快照，官方资讯 1 条进入审核、3 条未变化，没有自动公开。
- 下一次自动执行：2026-07-28 04:35:30 CST。

### 发布

- 当前生产 release：`20260727224653`，上一版本：`20260727222030`。
- 最终备份：`predeploy-content-monitor-20260727223518.sql.gz`，恢复验证通过。
- 微信开发版本 `0.1.24` preview/upload 成功，二维码：`output/wechat-preview/readiness-0.1.24-20260727222030.png`。
- 真机验收清单：`微信0.1.24真机验收清单 20260727.md`。

### 尚未解除的外部门槛

- 微信 AppSecret 必须确认已轮换。
- 阿里云真实邮件和短信尚未接入，学校认证不能作为正式上线闭环宣称完成。
- `0.1.24` 尚需在微信公众平台手动设为体验版并按清单完成 iPhone/Android 真机验收。

## 23:00 GitHub 代码交接

- 推送前补齐 `.gitignore`：排除 `.deploy/`、私钥、数据库、运行时存储、PID/错误文件、Playwright/测试输出、根目录截图和本地参考素材。
- 从版本控制中移除 `frontend/project.private.config.json`、根目录 `project.private.config.json` 和 `server/.api.pid`，本地文件可继续使用但不进入仓库。
- 暂存范围包含当前 React/Vite 用户端、Express API、微信壳、管理员端、学校数据、官方内容巡检、生产部署模板、自动化测试、商业计划和中文实验日志。
- 推送前扫描未发现微信 AppSecret、SSH 私钥正文、云访问密钥或真实管理员密码；`.env.example` 仅保留明确占位值。
- 最后验证基线：`npm run verify:local-release` 通过，生产 release `20260727224653` 健康，微信开发版本 `0.1.24` 已上传。
